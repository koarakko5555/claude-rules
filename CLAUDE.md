# CLAUDE.md

このファイルはセッション開始時に Claude Code に自動で読み込まれます。

## プロジェクト概要

**{{PROJECT_NAME}}** — {{PROJECT_DESCRIPTION}}

## アーキテクチャ

- 言語/ランタイム: {{STACK}}
- 主要ディレクトリ:
{{TOP_DIRS}}

## 開発ルール

- 言語・ドメイン固有の規約は `.claude/rules/` を参照すること。
- 新規ファイル作成より既存ファイルの編集を優先する。
- コメントは原則書かない。書く場合は WHY（自明でない制約・回避策・意外な不変条件）のみ。
- 後方互換のためのシム・未使用コードの残骸は残さない。

## 実行方法

{{RUN_COMMANDS}}

## 重要な前提知識

<!-- コードから読み取れない、Claude が知っておくべき背景情報をここに記述 -->
